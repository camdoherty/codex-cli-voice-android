interface:
  display_name: "Voice Session"
  short_description: "Run a local Android STTS voice turn in Termux"
  default_prompt: "Use $stts talk for one voice turn, stts session to open the tmux workspace, or sh scripts/stts-session.sh say \"text\" for one-shot speech."

policy:
  allow_implicit_invocation: false
