interface:
  display_name: "Voice Session"
  short_description: "Start a live voice loop or speak text aloud in Termux"
  default_prompt: "Use $stts start for a live voice session, or sh scripts/stts-session.sh say \"text\" for one-shot speech."

policy:
  allow_implicit_invocation: false
